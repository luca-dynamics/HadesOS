# Runtime

Local runtime foundation for device context, encrypted memory, approvals, local execution, and offline-safe behavior. Future implementation should start from the safety contracts in `docs/security`.
